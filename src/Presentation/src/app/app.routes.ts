import { Routes } from '@angular/router';
import { LocationSearchComponent } from './features/location-search/components/location-search.component';

export const routes: Routes = [
  { path: '', component: LocationSearchComponent }
];
